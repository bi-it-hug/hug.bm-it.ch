/*
var header = document.getElementsByTagName("header")[0]
var delay = 50

window.addEventListener("scroll", function() {
    var initPos = window.scrollY

    setTimeout(function() {
        if (window.scrollY > initPos) {
            header.classList.add("retracted-header")
    
        } else if (window.scrollY < initPos) {
            header.classList.remove("retracted-header")
        }
    }, delay)
})
*/