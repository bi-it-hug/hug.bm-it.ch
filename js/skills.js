/*
var pythonImage = document.getElementById("pythonImage")
var pythonBar = document.getElementById("pythonBar")
var pythonPercentage = document.getElementById("pythonPercentage")
var counter = 0
var timer

var languages = document.querySelectorAll(".language")

languages.forEach((element) => {
    var divElement = element.querySelector("div")

    divElement.addEventListener("mouseenter", (event) => {
        event.currentTarget.style.height = "10em"
    })

    divElement.addEventListener("mouseleave", (event) => {
        event.currentTarget.style.height = "0em"
    })    
})
*/